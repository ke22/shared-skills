portability_policy:
  core_is_single_source_of_truth: true
  wrapper_must_be_thin: true
  wrapper_metadata_required: true
  mutable_state_inside_skill_folder: false
  preferred_tool_contracts:
    - json_schema
    - mcp
    - openapi_when_enterprise_governance_requires_it
